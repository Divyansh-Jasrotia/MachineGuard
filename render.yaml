services:
  - type: web
    name: machineguard
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 7860
      - key: LLM_PROVIDER
        value: none
      - key: GNANI_API_KEY
        sync: false
